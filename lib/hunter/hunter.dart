class Hunter {
  String first, last, email, company, position, domain, uid;
  int confidence;
  Hunter(
      {this.uid,
      this.company,
      this.confidence,
      this.domain,
      this.email,
      this.first,
      this.last,
      this.position});
}
