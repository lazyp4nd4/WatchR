class Hunter {
  String first, last, email, company, position, domain;
  int confidence;
  Hunter(
      {this.company,
      this.confidence,
      this.domain,
      this.email,
      this.first,
      this.last,
      this.position});
}
